export interface PhotoAgentData {
  productImage: string; // base64 encoded image string
  logoImage?: string; // base64 encoded image string, optional
  productBrandName: string;
  hexColor?: string; // optional
}
